// config/feeds.ts —— 源白名單入口（spec §9）
// Canonical 定義喺 shared/src/feeds.ts（前端同 scripts 共用同一份，避免兩處改走位）。
export { FEEDS } from "../shared/src/feeds";
export type { FeedSource } from "../shared/src/feeds";